# Leave blank to auto-derive from the page's hostname (works for both
# localhost AND opening from your phone at http://192.168.x.x:5173).
#
# Set explicitly only if you have a custom setup:
# VITE_API_URL=http://localhost:4000
# VITE_SOCKET_URL=http://localhost:4000
